[build]
  command = "npm run build"
  publish = "dist"

# Client-side routes (BrowserRouter). Static assets under dist/ are served when they exist.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
